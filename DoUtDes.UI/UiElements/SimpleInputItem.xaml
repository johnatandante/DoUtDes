<UserControl x:Class="DoUtDes.UiElements.SimpleInputItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="71" d:DesignWidth="467">
    
    <Grid x:Name="LayoutRoot" MinHeight="48">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="Title" Text="{Binding Label}" MinHeight="24" Grid.Row="0" />
        <TextBox x:Name="Value" Text="{Binding TextValue}" MinHeight="24" MaxHeight="48" Grid.Row="1" />
    </Grid>
</UserControl>
